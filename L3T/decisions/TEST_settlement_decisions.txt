# Settlement decisions are possible vs _all_ settlements and are shown in the Settlement Diplomacy View, not the Intrigue View. The taker is in the FROM scope.
#
# filter = [capital/owned/vassal_owned/sub_realm_owned/realm_owned/dynasty_owned/all]
# ai_target_filter = [capital/owned/vassal_owned/sub_realm_owned/realm_owned/dynasty_owned/all] (which settlements for which the AI evaluates the decision.)
#	owned: all settlements owned by the AI
#	vassal_owned: all settlements owned by direct vassal rulers of the AI's employer
#	sub_realm_owned: all settlements below the AI's employer
#	realm_owned: all settlements in the same top realm as the AI
#	dynasty_owned: all settlements owned by members of the same dynasty
#	all: all settlements (Avoid if possible. VERY CPU-HEAVY!)
#	

settlement_decisions = {

	convert_city_to_castle = {
		#only_playable = yes
		filter = owned
		ai_target_filter = owned
		is_high_prio = yes
		
		from_potential = {
			#is_playable = yes
			#is_feudal = yes
			#higher_tier_than = BARON
			is_patrician = no
			is_merchant_republic  = no
			not = { has_global_flag = apocalypto_finished }
			not = { year = 1186 }
		}
		
		potential = {
			#no spec yet
			#no merchant republic
			
			#no c_level rep
			holding_type = city
			holder_scope = {
				character = FROM
				is_patrician = no
				is_merchant_republic  = no				
			}
			is_capital = no
			NOT = { has_title_flag = saved_from_apocalypto }
		}
		allow = {
			location = {
				religion = FROM
			}
		}

		effect = {
				if = {
					limit = {
						location = {
							any_demesne_title  = {
								NOT = { count = 2 }
								tier = baron
								holding_type = city
								NOT = { has_title_flag = saved_from_apocalypto }
								#holder_scope = {
								#	is_patrician = no
								#	is_merchant_republic  = no
								#}
							}
						}
					}
					convert_to = CASTLE 
					FROM = { set_government_type = feudal_government }	
					#FROM = {death = { death_reason = DEATH_MISSING }}
					#recalc_succession = yes
					#random_list = {
					#	75 = {
					#		convert_to = CASTLE 
					#		holder_scope = { set_government_type = feudal_government }
					#		}
					#	25 = {
					#	}
					#}
				}
			if = {
					limit = {
						location = {
							any_province_holding = {
								count = 2
								tier = baron
								holding_type = city
								#holder_scope = {
								#	is_patrician = no
								#	is_merchant_republic  = no
								#}
							}
						}
					}
					FROM = {
						limit = {
							random_demesne_title  = {
								tier = baron
								holding_type = city
								#holder_scope = {
								#	is_patrician = no
								#	is_merchant_republic  = no
								#}
							}
						}
						convert_to = CASTLE
						holder_scope = { set_government_type = feudal_government }
					}

					FROM = {
						limit = {
							random_demesne_title  = {
								tier = baron
								holding_type = city
								#holder_scope = {
								#	is_patrician = no
								#	is_merchant_republic  = no
								#}
							}
						}
						set_title_flag = saved_from_apocalypto
					}					
				}
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}